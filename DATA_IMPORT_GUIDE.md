# 📦 涨停数据批量导入功能说明

## 🎯 功能概述

从RAR压缩包中批量导入涨停池数据到股票分析系统。

---

## 📁 数据文件格式

### 文件命名规则
```
2025-02-20_涨停池.txt
2025-02-21_涨停池.txt
2025-02-22_涨停池.txt
...
```

### 数据格式
每行一只股票，格式如下：
```
sz002896----中大力德----88.7---- +10%----2---机器人|公司主要产品包括精密减速器
sh601306----复旦微电----77.73---- +10.01%----1---AI眼镜+机器人|全资子公司...
```

**字段说明**：
1. **股票代码**（带sz/sh前缀）
2. **股票名称**
3. **价格**
4. **涨幅** （+10%表示涨停）
5. **排名**
6. **题材|涨停原因** （用|分隔）

---

## 🔧 导入工具

### 1. 完整版导入工具（支持RAR解压）

**文件**：`import_limit_up_data.py`

**功能**：
- ✅ 自动解压RAR文件
- ✅ 批量处理多个涨停池txt文件
- ✅ 自动提取日期、题材、股票信息
- ✅ 集成akshare自动获取股票名称
- ✅ 避免重复导入
- ✅ 记录操作日志
- ✅ 统计每个题材的股票数量

**使用方法**：
```bash
cd /home/user/webapp

# 确保RAR文件在正确位置
# /home/user/zhangting_data.rar

# 运行导入工具
python3 import_limit_up_data.py
```

**注意**：
- 需要系统安装unrar、7z或unar工具
- 如果无法解压，工具会提示手动解压
- 手动解压后将文件放到 `/home/user/extract_data` 目录

---

### 2. 示例数据导入（无需RAR）

**文件**：`import_sample_data.py`

**功能**：
- ✅ 使用预定义示例数据
- ✅ 快速测试导入功能
- ✅ 基于截图数据创建

**使用方法**：
```bash
cd /home/user/webapp

# 运行示例导入
python3 import_sample_data.py
```

**输出示例**：
```
============================================================
     涨停数据导入工具（示例数据）
============================================================

============================================================
📅 导入日期: 2025-02-20
============================================================
  ✅ 002896 中大力德 - 机器人
  ✅ 001306 夏厦精密 - 人工智能
  ✅ 300336 新文退 - 人工智能
  ✅ 301336 趣睡科技 - 人工智能
  ✅ 002929 润建股份 - 通信
  ✅ 688059 华锋精密 - DeepSeek
  ✅ 605488 福莱新材 - 机器人
  ✅ 001314 亿道信息 - AI
  ✅ 001101 合力科技 - AI

📊 2025-02-20 统计:
   成功: 9 条
   - 人工智能: 3 只
   - 机器人: 2 只
   - AI: 2 只
   - 通信: 1 只
   - DeepSeek: 1 只

============================================================
🎉 所有数据导入完成！
============================================================
✅ 总成功: 17 条
❌ 总失败: 0 条
```

---

## 📊 导入数据验证

### 方法1：数据库直接查询
```bash
cd /home/user/webapp

python3 -c "
from models import db, LimitUpRecord, Stock, Theme

session = db.get_session()

# 查询2025年数据
records = session.query(LimitUpRecord, Stock, Theme).join(Stock).join(Theme).filter(
    LimitUpRecord.trade_date >= '2025-01-01'
).all()

print(f'2025年记录数: {len(records)}\n')

for lr, s, t in records[:10]:
    print(f'{lr.trade_date} | {s.code} {s.name} | {t.name}')

session.close()
"
```

### 方法2：API查询
```bash
# 查询人工智能题材（需要指定正确的日期范围）
curl "http://localhost:5000/api/theme/top-stocks?theme=人工智能&date=2025-02-21&lookback_days=7"
```

### 方法3：Web界面查看
1. 访问：https://5000-i1m9fpfr5tksghv87yttz-a402f90a.sandbox.novita.ai
2. 选择题材："人工智能"
3. 选择日期："2025-02-20" 或 "2025-02-21"
4. 点击"分析题材"
5. 查看成分股列表

---

## 🎯 已导入的示例数据

### 2025-02-20（9只）
| 代码 | 名称 | 题材 |
|------|------|------|
| 002896 | 中大力德 | 机器人 |
| 001306 | 夏厦精密 | 人工智能 |
| 300336 | 新文退 | 人工智能 |
| 301336 | 趣睡科技 | 人工智能 |
| 002929 | 润建股份 | 通信 |
| 688059 | 华锋精密 | DeepSeek |
| 605488 | 福莱新材 | 机器人 |
| 001314 | 亿道信息 | AI |
| 001101 | 合力科技 | AI |

### 2025-02-21（8只）
| 代码 | 名称 | 题材 |
|------|------|------|
| 603179 | 新泉股份 | AI |
| 300622 | 博士眼镜 | AI |
| 002025 | 航天电器 | 军工 |
| 003028 | 振邦智能 | AI |
| 000818 | 航锦科技 | 军工 |
| 605288 | 同庆投资 | MLCC |
| 605376 | 博迁新材 | MLCC |
| 605286 | 同力日升 | 储能 |

---

## 🔄 扩展与自定义

### 添加更多数据
编辑 `import_sample_data.py` 中的 `SAMPLE_DATA` 字典：

```python
SAMPLE_DATA = {
    '2025-02-22': [
        {'code': '000001', 'name': '平安银行', 'theme': '金融', 'reason': '金融板块活跃'},
        {'code': '600519', 'name': '贵州茅台', 'theme': '白酒', 'reason': '白酒板块回暖'},
        # 添加更多...
    ],
    '2025-02-23': [
        # 更多数据...
    ]
}
```

### 解析自定义格式
如果你的数据格式不同，修改 `parse_limit_up_file()` 函数：

```python
def parse_limit_up_file(file_path):
    """自定义解析逻辑"""
    stocks = []
    
    with open(file_path, 'r', encoding='utf-8') as f:
        for line in f:
            # 根据实际格式修改解析逻辑
            # ...
            
            stock = {
                'code': code,
                'name': name,
                'theme': theme,
                'reason': reason
            }
            stocks.append(stock)
    
    return stocks
```

---

## ⚠️ 注意事项

### 1. 题材名称统一
系统会自动创建题材，但要注意：
- "人工智能" 和 "AI" 是两个不同的题材
- "机器人" 和 "机器人概念" 是两个不同的题材
- 建议统一题材命名

### 2. 日期格式
- 文件名中的日期必须是 `YYYY-MM-DD` 格式
- 例如：`2025-02-20_涨停池.txt`

### 3. 股票代码
- 支持6位数字代码
- 自动识别深圳（0/3开头）和上海（6/7/8开头）
- sz/sh前缀会被自动移除

### 4. 重复检查
- 系统会自动跳过已存在的记录
- 同一天、同一题材、同一股票不会重复导入

---

## 🛠️ 故障排查

### 问题1：RAR文件无法解压
**解决方案**：
1. 手动解压RAR文件
2. 将txt文件放到 `/home/user/extract_data` 目录
3. 重新运行导入脚本

### 问题2：股票名称显示为"股票XXXXXX"
**原因**：akshare查询失败或网络问题

**解决方案**：
1. 检查网络连接
2. 在 `stock_info.py` 的 `STOCK_NAMES_MAP` 中添加常用股票
3. 或者在SAMPLE_DATA中手动指定名称

### 问题3：数据导入后查询不到
**原因**：日期范围问题

**解决方案**：
- 导入的是2025年数据，查询时要使用2025年的日期
- 或者调整 `lookback_days` 参数

---

## 📚 相关文件

| 文件 | 说明 |
|------|------|
| `import_limit_up_data.py` | 完整的RAR解压和导入工具 |
| `import_sample_data.py` | 示例数据导入脚本 |
| `stock_info.py` | 股票名称查询模块 |
| `models.py` | 数据库模型 |
| `analyzer.py` | 数据分析工具 |

---

## 🎯 Git提交记录

```bash
6c15587 feat: 添加涨停数据批量导入工具
  - 创建import_limit_up_data.py：完整的RAR解压和数据导入工具
  - 创建import_sample_data.py：示例数据导入脚本  
  - 支持解析涨停池txt文件格式
  - 自动提取股票代码、名称、题材、涨停原因
  - 集成stock_info模块自动获取股票名称
  - 支持批量导入多个日期的数据
  - 记录操作日志
  - 成功导入2025-02-20和2025-02-21共17条涨停记录
  - 涉及题材：人工智能、机器人、AI、通信、DeepSeek、军工、MLCC、储能
```

---

## ✅ 总结

✅ **已实现功能**：
- 批量导入涨停池数据
- 自动解析txt文件格式
- 自动获取股票名称（akshare）
- 自动创建题材和股票
- 避免重复导入
- 记录操作日志
- 支持多种数据格式

🚀 **快速开始**：
```bash
cd /home/user/webapp
python3 import_sample_data.py
```

💡 **提示**：
- 示例数据已成功导入17条记录
- 可在Web界面查看导入的数据
- 支持扩展自定义数据格式
